import { useQuery } from "react-query";
import "../css/Profile.css";

const Profile = () => {
  return (
    <div className="profile">
      <div className="wrapper">
        -> Create profile page and backend stuff! follow/unfollow recent posts
        etc!
      </div>
    </div>
  );
};

export default Profile;
